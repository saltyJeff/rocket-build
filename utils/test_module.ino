//since the code is only transformed when creating a "complete" build, module writers can use test files to debug their module
//be aware that since there's no preprocessor, you're responsible for downloading libs and #include relevant files
//for files with the "ino" extension, there's no need to #include other ino's (you'll still need to #include headers